# The torch/torchvision below run on the CPU (works everywhere, but slower).
#
# Have an NVIDIA GPU? Get a big speedup by installing the GPU version instead:
#   1. Skip the two torch lines below (leave the rest of this file as-is).
#   2. Run this command:
#        pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu126
#
# That's it. camera.py uses the GPU automatically when the GPU version is installed.
torch==2.12.0
torchvision==0.27.0
opencv-python==4.13.0.92
mediapipe==0.10.35
numpy==2.4.4
pandas==3.0.2
pillow==12.2.0
requests==2.33.1
tqdm==4.67.3
transformers==5.12.1
python-osc